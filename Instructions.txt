	ALWAYS OPEN INSPECT ELEMENT
for calling API - network tab
for calling log - console tab

Basic CRUD Operation 

Teacher CRUD

C = Create
R = Read
U = Update
D = Delete

Teacher CRUD APIs

* useEffect() Hooks
	- useEffect is a function used for whenever we load a component if we want to call some APIs then we can use the useEffect hooks.


oldaoo.map((cv,idx,arr)=>{
	return oldaoo
})

Action Buttons
1. Delete
2. Update
3. View

button is button
anchor (a) is button
input type="button" is a button

onClick

Traversing (Travel)
Parent ---> Child (Traversing Down)
Child  ---> Parent (Traversing Up)

